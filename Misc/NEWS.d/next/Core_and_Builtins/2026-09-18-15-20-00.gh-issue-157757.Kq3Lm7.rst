Fix a lazy ``import a.b as c`` reading ``b`` off ``a`` instead of importing
the module ``a.b``.  It now binds the submodule, including when ``a.b`` is
already imported and carries an attribute named ``b``, and raises
:exc:`ModuleNotFoundError` when no module backs the name.
